" Trigger keys
let g:qs_highlight_on_keys = ['f', 'F', 't', 'T']

" Char limit
let g:qs_max_chars=150

" Don't trigger in this
let g:qs_buftype_blacklist = ['terminal', 'nofile']

" Don't hightlight before hit keys
let g:qs_lazy_highlight = 1
